---
description: Run the Hospital Agent CLI
---

// turbo
1. Ensure the Python environment is ready.
   `uv sync`

// turbo
2. Run the Hospital Agent CLI directly.
   `uv run python hospital_agent.py`
