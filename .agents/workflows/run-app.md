---
description: Run the Hospital Queue Agent (Streamlit App)
---

// turbo
1. Ensure the Python environment is ready and dependencies are synced.
   `uv sync`

// turbo
2. Start the Streamlit application on port 5000.
   `uv run streamlit run app.py --server.port 5000`
