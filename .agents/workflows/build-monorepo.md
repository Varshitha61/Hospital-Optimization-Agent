---
description: Build and Typecheck the TypeScript Monorepo
---

// turbo
1. Install dependencies using pnpm.
   `pnpm install`

// turbo
2. Run Typecheck for all packages using the root script.
   `pnpm run typecheck`

// turbo
3. Build the entire monorepo.
   `pnpm run build`
